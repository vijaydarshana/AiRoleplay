/**
 * @deprecated Use @/backend/models/session.model instead.
 * Re-exports from the backend model layer for backward compatibility.
 */
export {
  STORAGE_KEY,
  PENDING_SESSION_KEY,
  PENDING_PROTOCOL_KEY,
  SELECTED_SCENARIO_KEY,
  VIEW_SESSION_KEY,
  MAX_SESSIONS,
} from '@/backend/models/session.model';
